<div id="wrapper">
  <div class="col-lg-10">
    <h2 class= "pull-left"> Projektmitglieder hinzufügen</h2>
  </div>

  <div class="wrapper wrapper-content  animated fadeInRight">
    <div class="row" >
      <div class="ibox ">
        <div class="ibox-title">
          Schritt für Schritt ein neues Projekt erstellen
        </div>
        Projektmitglieder:</br>

        <% @projectMembers.each do |packet| %>
            <%first_name%> <%last_name%> <%email%>
            </br>
        <%end%>
        <%= form_for(:project, as: :project, url: { action: 'add_user' }) do |f| %>
            <%= search_field(:user, :name, :autofocus => true ) %>
            <%= f.hidden_field :projects_id, :value => params[:id] %>
            <%= f.submit "add", :class=>'btn btn-outline btn-primary dim pull-right' %>

        <% end %>

          </div>
        </div>
      </div>
    </div>


